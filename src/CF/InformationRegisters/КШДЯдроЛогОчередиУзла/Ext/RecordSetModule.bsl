#Область ОбработчикиСобытий

Процедура ПередЗаписью(Отказ, Замещение)
	Для Каждого Запись Из ЭтотОбъект Цикл
		Если Не ЗначениеЗаполнено(Запись.ПриведеннаяДата) Тогда
			Милли = 1000;
			Добавка = ТекущаяДатаСеанса() - ТекущаяУниверсальнаяДата();
			Запись.ПриведеннаяДата = '00010101' + Цел(Запись.ДатаСобытия / Милли) + Добавка;
			Запись.Миллисекунды = Запись.ДатаСобытия - Цел(Запись.ДатаСобытия / Милли) * Милли;
		КонецЕсли;
	КонецЦикла;
КонецПроцедуры

#КонецОбласти
